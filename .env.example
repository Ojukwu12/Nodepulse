# NodePulse environment variables example
# Copy this file to `.env` and update values for your environment


# NodePulse environment variables example
# Copy this file to `.env` and update values for your environment

NODE_ENV=development
PORT=4000

# MongoDB connection string (REQUIRED in production)
# Example: mongodb://username:password@host:27017/nodepulse
MONGO_URI=mongodb://localhost:27017/nodepulse

# JWT secret used to sign access tokens (REQUIRED in production)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# Chain RPC (Gensyn / EVM-compatible). Do NOT embed API keys in code.
# Example: https://mainnet.infura.io/v3/<PROJECT_ID>
CHAIN_RPC_URL=https://your-evm-rpc.example
CHAIN_CONFIRMATIONS=12
# Gensyn contract address (not secret). Set to the Gensyn contract address you monitor.
CHAIN_CONTRACT_ADDRESS=0x69C6e1D608ec64885E7b185d39b04B491a71768C

# Optional: path to a local JSON file containing the Gensyn contract ABI.
# If provided, NodePulse will load the ABI and attempt to decode events.
# Example: GENSYN_ABI_PATH=./config/gensyn.abi.json
GENSYN_ABI_PATH=

# Optional: comma-separated event names to filter (requires ABI loaded).
# Example: CHAIN_EVENT_NAMES=JobCompleted,RewardPaid
CHAIN_EVENT_NAMES=

# Optional: comma-separated topics (hex strings) to filter logs when ABI is not available.
# Example: CHAIN_EVENT_TOPICS=0x1234...,0x5678...
CHAIN_EVENT_TOPICS=

# Sidecar API key secret (used to sign/verify sidecar keys). Treat as secret.
SIDECAR_KEY_SECRET=some_random_secret

# Redis URL (if using BullMQ/workers). Optional for simple dev setups.
REDIS_URL=redis://localhost:6379

# Logging / monitoring
LOG_LEVEL=info
SENTRY_DSN=

